name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
   runs-on: ubuntu-latest
   steps:
    - name: Print greeting
      run: each "Hello World"
    - name: print goodby
      run: echo "done - bye!"

      
